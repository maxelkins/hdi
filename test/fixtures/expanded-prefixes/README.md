# multi-ecosystem

## Installation

Run `mix deps.get` to fetch Elixir dependencies.

Use `composer install` for PHP packages.

Then `flutter pub get` for mobile deps.

```bash
npm install
```

## Usage

Run `just serve` to start the dev server.

Use `conda activate myenv` for the Python environment.
