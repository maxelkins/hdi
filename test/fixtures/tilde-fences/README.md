# tilde-app

## Installation

~~~bash
pip install tilde-app
~~~

## Usage

~~~
tilde-app run --port 8080
~~~

## Build

Backtick block still works:

```bash
tilde-app build
```

Tilde block with skipped language:

~~~json
{"key": "value"}
~~~
