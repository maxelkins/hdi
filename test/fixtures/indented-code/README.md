# old-project

## Installation

Install the package:

    pip install old-project
    pip install -r requirements.txt

## Usage

Run the server:

    python manage.py runserver

Some indented prose that is NOT a command:

    This is just a note about the system.
    It spans multiple lines.
