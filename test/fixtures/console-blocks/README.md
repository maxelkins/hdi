# cli-tool

## Installation

```console
$ npm install -g cli-tool
npm added 42 packages in 2.1s
$ cli-tool init
Initialized new project in ./my-project
```

## Usage

```console
$ cli-tool build
Building...
Done in 0.8s
```

Regular bash block still works:

```bash
cli-tool serve
```
